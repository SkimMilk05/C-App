<%= form_with(model: blue_area, local: true) do |form| %>
  <% if blue_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blue_area.errors.count, "error") %> prohibited this blue_area from being saved:</h2>

      <ul>
        <% blue_area.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
