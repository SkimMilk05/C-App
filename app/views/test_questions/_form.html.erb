<%= form_with(model: test_question, local: true) do |form| %>
  <% if test_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_question.errors.count, "error") %> prohibited this test_question from being saved:</h2>

      <ul>
        <% test_question.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
